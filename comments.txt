        //fgets(buffer, size, file)
        //buffer is where to store the data
        //size is the maximum size in bytes
        //file is where to read from
        //the stored data has a newline character


    //100 is the maximum numbers of line
    //1024 is the maximum characters on each lines


    //open in read mode

    //checks for error
    //!file is the same as file != 0

//gcc -shared -o c.dll main.c
//only works on window